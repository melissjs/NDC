export class Election {
  electionKey?: string;
  cIId?: string;
  electionTitle: string;
  electionDay: string;
  electionType: string;
  ocdDivisionId?: string;
  previousElection?: string;
  active: boolean;
}
